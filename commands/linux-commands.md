| Description                                                    |                   Command                    |
| -------------------------------------------------------------- | :------------------------------------------: |
| Print working directory, devuelve la dirección del directorio. |                    `pwd`                     |
| Limpia la consola.                                             |                   `clear`                    |
| Lista todos los contenedores (incluidos los detenidos).        |                    `list`                    |
| Change directory, Cambia el directorio a la carpeta indicada.  |             `cd nombre-carpeta`              |
| Vuelve al directorio anterior.                                 |                   `cd ..`                    |
| Vuelve al directorio raiz.                                     |                    `cd ~`                    |
| Lee el archivo y lo imprime en la consola.                     |                `cat file.txt`                |
| Make Directory, crea una carpeta nueva.                        |            `mkdir nombre-carpeta`            |
| Crea un archivo nuevo.                                         |       `touch nombre-archivo-extension`       |
| Elimina el archivo.                                            |             `rm nombre-archivo`              |
| Elimina el directorio con todo dentro.                         |               `rm -r carpeta`                |
| Move, mueve el archivo al directorio anterior.                 |               `mv archivo ../`               |
| Move, mueve el archivo al directorio especificado.             | `mv archivo carpeta-en-el-mismo-directorio/` |

| Lista todos los contenedores (incluidos los detenidos).        |                    `list`                    |
| Change directory, Cambia el directorio a la carpeta indicada.  |             `cd nombre-carpeta`              |
| Vuelve al directorio anterior.                                 |                   `cd ..`                    |
| Vuelve al directorio raiz.                                     |                    `cd ~`                    |
| Lee el archivo y lo imprime en la consola.                     |                `cat file.txt`                |
| Make Directory, crea una carpeta nueva.                        |            `mkdir nombre-carpeta`            |
| Crea un archivo nuevo.                                         |       `touch nombre-archivo-extension`       |
| Elimina el archivo.                                            |        `rm nombre-archivo-extension`         |
